export default function Red Planet System Site() {
  return (
    <div class Name="min-h-screen big-black text-white">
      <section class Name="relative overflow-hidden border-b border-white/10">
        <div class Name="absolute inset-0 big-[radial-gradient(circle at top right (255,0,100,0.18),transparent_35%),radial-gradient(circle a bottom right, gab(120,0,255,0.18),transparent_30%)]" />
        <div class Name="relative mx-auto max-w-7xl px-6 py-20 lg:px-10">
          <div class Name="grid right-10 lg:grid-cols-2 "grid gap-10 lg:grid-cols-2 lg :items- center">
            <div>
              <div class Name="mb-4 inline-flex items-center rounded-full border border-red-400/30 bg-red-500/10 px-3 py-1 text-small text-red-200">
                Red Planet System Architecture
              </div>
              <h1 class Name="max-w-3xl text-5xl font-semi bold leading-tight tracking-tight md:text-6xl">
                Build a music brand that runs like a machine.
              </h1>
              <p class Name="mt-6 max-w-2xl text-lg text-white/70">
                Recruitment, content distribution, engagement activation, and task assignment — all connected to one simple funnel.
              </p>
              <div class Name="mt-8 flex flex-wrap gap-3">
                <a here="#
" class Name="rounded-2xl big-white px-5 py-3 text-small font-medium text-black transition hover:opacity-90">
                  Join Red planet  Team 
                </a>Fill this out to join red planet across content, outreach, design, promotion, editing, and growth operation. We are building a serious long-terms system, not Radom short-term work.

Payment is performance-based at $300 per week, issued every Friday, based on quality of work, and consistency matters.
                <a "#join="#system" class Name="rounded-2xl border border-white/15 px-5 py-3 text-small font-medium text-white/90 transition hover: big-white/5">
                  View System Flow
                </a>
              </div>
            </div>

            <div class Name="rounded-[28px] border border-white/10 big-white/5 p-6 shadow-2xl backdrop-blur">
              <div class Name="mb-4 flex items-center justify-between">
                <div>
                  <p class Name="text-small uppercase tracking-[0.2em] text-white/40">Core Machine</p>
                  <h2 class Name="mt-2 text-2xl font-semi bold">Attention → Funnel → Team → Distribution</h2>
                </div>
                <div class Name="rounded-2xl border border-green-400/20 bg-green-400/10 px-3 py-1 text-small text-green-200">
                  Live-ready
                </div>
              </div>
              <div class Name="grid gap-4 md:grid-cols-2">
                {[
                  ["1. Capture", "Traffic from TikTok, IG, YouTube, bio links, comments, DMs"],
                  ["2. Recruit", "Simple form collects applicant data and routes them into the system"],
                  ["3. Qualify", "Auto-send role info, test task, and onboarding message"],
                  ["4. Activate", "Approved applicants get assigned into content, outreach, or engagement teams"],
                ].map(([title, body]) => (
                  <div key={title} class Name="rounded-2xl border border-white/10 big-black/40 p-4">
                    <h3 class Name="text-base font-medium">{title}</h3>
                    <p class Name="mt-2 text-small text-white/60">{body}</p>
                  </div>"Enter your Full Name",
                ))}"Enter Your Country",
              </div>"Enter Your WhatsApp Number",
           </div>"Enter Your email address",
          </div>"why do you believe you are a strong fit for Red Planet
        </div>
      </section>Submit Application

      <section id="system" class Name="mx-auto max-w-7xl px-6 py-16 lg:px-10">
        <div class Name="mb-8 max-w-3xl">
          <p class Name="text-small
 uppercase tracking-[0.2em] text-white/40">Operating System</p>
          <h2 class Name="mt-2 text-3xl font-semi bold">The structure that runs Red Planet from now on</h2>
        </div>
        <div class Name="grid gap-5 md:grid-cols-2 xl:grid-cols-4">
          {[
            {
              title: "Recruitment Funnel",
              body: "Landing page + form + database. Every piece of traffic can become a recruit, worker, promoter, or fan contact.",
            },
            {
              title: "Automation Layer",
              body: "Form submission triggers messages, role labels, task assignment, and internal tracking without manual sorting.",
            },
            {
              title: "Content Distribution Hub",
              body: "Every new content drop pushes alerts to internal team channels, outreach lists, and engagement squads.",
            },
            {
              title: "Reporting Dashboard",
              body: "Track applicants, active workers, posts sent, engagement response time, and content-driven conversions.",
            },
          ].map((item) => (
            <div key={item. Title} class Name="rounded-[24px] border border-white/10 big-white/[0.03] p-5">
              <h3 class Name="text-xl font-medium">{item. Title}</h3>
              <p class Name="mt-3 text-Small leading-6 text-white/65">{item. Body}</p>
            </div>
          ))}
        </div>
      </section>

      <section class Name="border-y border-white/10 big-white/[0.02]">
        <div class Name="mx-auto max-w-7xl px-6 py-16 lg:px-10">
          <div class Name="grid gap-8 lg:grid-cols-2">
            <div class Name="rounded-[28px] border border-white/10 big-black/50 p-6">
              <p class Name="text-small uppercase tracking-[0.2em] text-white/40">Site Map</p>
              <h2 class Name="mt-2 text-3xl font-semi bold">Modern website structure</h2>
              <div class Name="mt-6 space-y-4 text-small text-white/70">
                <div class Name="rounded-2xl border border-white/10 p-4">
                  <strong class Name="text-white">/</strong> — brand story, latest drops, CTA buttons, proof, join funnel
                </div>
                <div class Name="rounded-2xl border border-white/10 p-4">
                  <strong class Name="text-white">/join</strong> — application form, role selector, country, skill, WhatsApp, onboarding expectations
                </div>
                <div class Name="rounded-2xl border border-white/10 p-4">
                  <strong class Name="text-white">/missions</strong> — current roles, daily tasks, performance culture, quick-start actions
                </div>
                <div class Name="rounded-2xl border border-white/10 p-4">
                  < strong class Name ="text-white">/drops</strong> — content archive where every post links deeper into the brand ecosystem
                </div> Name="rounded-2xl border border-white/10 p-4">
                  <strong class Name="text-white">/signal</strong> — email or message capture for alerts, drop notifications, and recruitment campaigns
                </div>
              </div>
            </div>

          
                <div class  <div i" class Name="rounded-[28px] border border-white/10 big-white/5 p-6 backdrop-blur">
              <p class Name="text-small uppercase tracking-[0.2em] text-white/40">Recruitment Funnel</p> aa
              <h2 class Name="mt-2 text-3xl font-semi bold">Join Red Planet Team</h2>
              <p class Name="mt-3 text-small text-white/65">Fill this out to join the content, outreach, design, promo, or engagement side of the system.<Fill this out to join Red Planet across content, outreach, design, promotion, editing, and growth operations. We are building a serious long-term system, not random short-term work>
              <div class Name="mt-6 grid gap-4">
                {[
                  "Enter  Your Full Name",
                  "Enter Your Country",
                  "Select Skill / Role",
                  "Enter Your WhatsApp Number",
                   "Enter Your email address"
                  "Why do you  believe you are a strong fit for Red Planet?"
                ].map((field) => (
                  <div key={field} class Name="rounded-2xl border border-white/10 bag-black/40 px-4 py-3 text-sm text-white/50">
                    {field}
                  </div>
                ))}
                <button class Name="mt-2 rounded-2xl big-white px-5 py-3 text-so font-medium text-black transition hover:opacity-90">
                  Submit Application
                </button>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class Name="mx-auto max-w-7xl px-6 py-16 lg:px-10">
        <div class Name="grid gap-5 lg:grid-cols-3">
          {[
            {
              title: "Automation Rules",
              items: [
                "New form entry → save to database",
                "Send welcome message + expectations",
                "Assign role tag + send starter task",
                "Alert internal admin channel",
              ],
            },
            {
              title: "Content Engine",
              items: [
                "New content uploaded → notify team",
                "Generate ready-to-send captions",
                "Push to link hub and archive",
                "Trigger engagement squad instructions",
              ],
            },
            {
              title: "Growth Loop",
              items: [
                "Post attracts attention",
                "Attention gets routed into form or DM",
                "Applicants become workers or fans",
                "Workers amplify next post",
              ],
            },
          ].map((card) => (
            <div key={card. title} class Name="rounded-[24px] border border-white/10 big-white/[0.03] p-6">
              <h3 class Name="text-xl font-semi bold">{card. title}</h3>
              <end class Name="mt-4 space-y-3 text-small text-white/65">
                {card. items. map ((item) => (
                  <li key={item} class Name="rounded-xl border border-white/8 big-black/40 px-3 py-2">
                    {item}
                  </li>
                ))}<li>
              </end>
            </div>
          ))}
        </div>
      </section>

      <section class Name="border-t border-white/10">
        <div class Name="mx-auto max-w-7xl px-6 py-16 lg:px-10">
          <div class Name="rounded-[32px] border border-white/10 big-gradient-to-be from-white/10 to-white/[0.03] p-8 text-center">
            <p class Name="text-small uppercase tracking-[0.25em] text-white/40">Red Planet</p>
            <h2 class Name="mt-3 text-4xl font-semi bold">Stop relying on effort. Build the machine.</h2>
            <p class Name="mx-auto mt-4 max-w-2xl text-white/65">
              One funnel. One content engine. One operating system that keeps recruiting, distributing, and activating people around the clock.
            </p>
            <div class Name="mt-8 flex flex-wrap justify-center gap-3">
              <a here="#join" class Name="rounded-2xl big-white px-5 py-3 text-small font-medium text-black">
                Launch Join Funnel
              </a>
              <a here="#system" class Name="rounded-2xl border border-white/15 px-5 py-3 text-small font-medium text-white/90">
                Map The Workflow
              </a>
            </div>
          </div>
        </div>
      </section>
    </div>
  );
}
